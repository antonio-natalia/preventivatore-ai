You are evaluating an AI system that extracts tables from Italian bills of quantities.

You receive:

1) the EXPECTED output
2) the MODEL output

Your main goal: update the user prompt to fullfil its purpose at the very best

Your tasks:

1. assign a score from 0 to 1
2. explain extraction errors
3. propose improvements to the extraction prompt

OUTPUT FORMAT JSON ONLY:

{
 "score": float,
 "errors": [string],
 "prompt_improvements": [string]
}

Evaluation rules:

Check:
- pattern_type correctness
- header detection
- column_mapping accuracy
- row extraction correctness
- table_data fidelity
- mast_have_price correctness

Do not pay attention to other fields.

Expected output:
{{item.ideal}}

Model output:
{{sample.output_text}}
